# Environment Configuration for Tutoriaz
# Copy this file to .env and update with your actual values

# Server Configuration
NODE_ENV=production
HOST=0.0.0.0
PORT=3030

# Base URL (use your actual domain or IP)
BASE_URL=http://your-server-ip:3030
# For production with domain:
# BASE_URL=https://tutoriaz.yourdomain.com

# Security
JWT_SECRET=change-this-to-a-long-random-secure-string

# Database Location (IMPORTANT: Store outside git directory)
# Default: ./database.sqlite (not recommended for production)
# Recommended: /var/lib/tutoriaz/database.sqlite or ~/tutoriaz-data/database.sqlite
DB_PATH=/var/lib/tutoriaz/database.sqlite

# HTTPS Configuration (Optional)
# HTTPS_KEY_PATH=/path/to/privkey.pem
# HTTPS_CERT_PATH=/path/to/fullchain.pem
